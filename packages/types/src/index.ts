/**
 * @dfood/types
 *
 * Shared TypeScript types used across admin, api, and mobile apps.
 *
 * This package is scaffolded in Phase 4. In Phase 5, common types from
 * each app will be migrated here:
 *   - UserRole, User, SanitizedUser (from apps/api/src/types/auth.ts, etc.)
 *   - ErrorResponse (duplicated in admin and mobile)
 *   - Restaurant, FoodItem, Order, Address, etc. (from mobile + api)
 *   - Standard API response wrappers
 */

export const VERSION = "0.1.0";
