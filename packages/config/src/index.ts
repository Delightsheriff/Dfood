/**
 * @dfood/config
 *
 * Shared configuration presets for the monorepo.
 *
 * Intended for:
 *   - ESLint shared config
 *   - TypeScript config helpers
 *   - Tailwind CSS shared theme/plugins
 */

export {}
